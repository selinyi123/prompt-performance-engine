{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/prompt-performance/2.0.0/readiness-manifest.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "benchmark_target", "authority_sources", "artifacts", "manifest_sha256"],
  "properties": {
    "schema_version": {"const": "2.0.0"},
    "benchmark_target": {
      "type": "object",
      "additionalProperties": false,
      "required": ["suite_id", "definition_sha256"],
      "properties": {
        "suite_id": {
          "type": ["string", "null"],
          "minLength": 1
        },
        "definition_sha256": {
          "type": ["string", "null"],
          "pattern": "^[0-9a-f]{64}$"
        }
      }
    },
    "authority_sources": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "benchmark_run_directories",
        "human_review_plan",
        "code_execution_plan",
        "visual_review_plan"
      ],
      "properties": {
        "benchmark_run_directories": {
          "type": "array",
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        },
        "human_review_plan": {
          "type": ["string", "null"],
          "minLength": 1
        },
        "code_execution_plan": {
          "type": ["string", "null"],
          "minLength": 1
        },
        "visual_review_plan": {
          "type": ["string", "null"],
          "minLength": 1
        }
      }
    },
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["kind", "path", "sha256"],
        "properties": {
          "kind": {
            "enum": [
              "benchmark_replicate",
              "human_review",
              "operational_verification",
              "code_execution",
              "image_review",
              "expert_review_coverage",
              "independent_reproduction",
              "defect_register",
              "claims_audit"
            ]
          },
          "path": {"type": "string", "minLength": 1},
          "sha256": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$"
          }
        }
      }
    },
    "manifest_sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    }
  }
}
