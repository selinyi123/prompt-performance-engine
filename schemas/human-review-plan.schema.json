{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://local.invalid/prompt-performance/2.0.0/human-review-plan.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "replicate_report", "run_directories", "evaluations", "reviews"],
  "properties": {
    "schema_version": {"const": "2.0.0"},
    "replicate_report": {"type": "string", "minLength": 1},
    "run_directories": {
      "type": "array",
      "minItems": 3,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    },
    "evaluations": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string", "minLength": 1}
    },
    "reviews": {
      "type": "array",
      "minItems": 3,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["packet", "key", "submission"],
        "properties": {
          "packet": {"type": "string", "minLength": 1},
          "key": {"type": "string", "minLength": 1},
          "submission": {"type": "string", "minLength": 1}
        }
      }
    },
    "adjudications": {
      "type": "object",
      "additionalProperties": {"enum": ["win", "tie", "loss"]}
    }
  }
}
